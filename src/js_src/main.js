$(document).ready(function() {

    // fancybox
    $(".fancybox").fancybox();

    // flexsliders
    $('.firsttype_slider_wrapper').flexslider({
        animation: "slide",
        animationLoop: false,
        itemWidth: 210,
        itemMargin: 5,
        minItems: 2,
        maxItems: 3
    });

});
