$(document).ready(function() {

    // fancybox
    $(".fancybox").fancybox();

    // flexsliders
    $('.firsttype_slider_wrapper').flexslider({
        animation: "slide",
        animationLoop: false,
        itemWidth: 316,
        itemMargin: 0,
        minItems: 3,
        maxItems: 3
    });

});
